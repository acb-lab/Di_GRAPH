# CLI commands

The `digraph` Typer application and its three sub-commands. Installed as the `digraph` entry point via `pyproject.toml`.

---

::: digraph.cli
    options:
      members:
        - app
        - run
        - validate
        - stage
      show_source: false
      show_root_heading: true
